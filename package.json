{
  "name": "km-barber",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KM-BARBER - Premium barber shop appointment booking platform",
  "scripts": {
    "create-admin": "node create-admin.js",
    "setup": "cd server && npm install && npx prisma generate && npx prisma migrate dev --name init && npm run seed && cd .. && node create-admin.js && cd client && npm install"
  }
}
